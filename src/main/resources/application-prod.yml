server.port: 8080

debug: true

spring:
  datasource:
    url: ${app.datasource.url}
    username: ${app.datasource.username}
    password: ${app.datasource.password}

  jpa:
    show-sql: true

  flyway:
    url: ${app.datasource.url}
    username: ${app.datasource.username}
    password: ${app.datasource.password}

    mvc:
      log-request:
        details: true

app:
  datasource:
    url: jdbc:postgresql://ec2-18-205-122-145.compute-1.amazonaws.com:5432/da7096jij7cl0p?sslmode=require
    username: ujypdifcqancvg
    password: a3f628faaede6d3320c8c3dc63fa8c1d672ad9c51b336769621966074232a6f0

logging:
  level:
      org:
        springframework: DEBUG